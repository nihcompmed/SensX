# SensX
SensX PyTorch


conda create -n sensx python=3.10 -y
conda activate sensx
conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
pip install transformers accelerate tqdm scipy numpy huggingface_hub



